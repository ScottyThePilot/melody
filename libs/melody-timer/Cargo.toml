[package]
name = "melody-timer"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4" }
futures = { version = "0.3" }
tokio = { version = "1", features = ["macros", "sync", "time"] }
