[package]
name = "melody-connect-four"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
uord = { git = "https://github.com/ScottyThePilot/misc-crates", version = "0.1.0", features = ["serde"] }
